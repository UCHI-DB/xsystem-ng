package xsystem.ng.Learning;

// import java.io.FileReader;
// import java.util.ArrayList;

// import com.mifmif.common.regex.Generex;
// import com.opencsv.CSVReader;

// import org.javatuples.Pair;

// import java.util.regex.Pattern;
// import java.util.regex.PatternSyntaxException;

// import jdk.nashorn.internal.runtime.regexp.RegExp;

public class LabelAssignment {
    public static void main(String[] args) {
        // String regexSource = "src/main/resources/Regex/yo.csv";
        // ArrayList<Pair<String, Generex>> regexType = new ArrayList<>();
        // try { 
        //     FileReader filereader = new FileReader(regexSource);
        //     CSVReader csvReader = new CSVReader(filereader);
        //     String[] nextRecord;
        //     while ((nextRecord = csvReader.readNext()) != null) { 
        //         System.out.println(new Generex("(" + nextRecord[2] + ")").random() + "  " + nextRecord[0] + "  " + nextRecord[1] );
        //         // regexType.add(Pair.with(nextRecord[2], new Generex("(" + nextRecord[3] + ")")));
        //     }
        //     csvReader.close();
        // } 
        // catch (Exception e) { 
        //     // e.printStackTrace(); 
        // }
        // // regexType.remove(0);
        // // for(Pair<String, Generex> pair : regexType){
        // //     String randomStr = pair.getValue1().random();
		// //     System.out.println(randomStr + "  " + pair.getValue0());
        // // } 
        
    }
    
}